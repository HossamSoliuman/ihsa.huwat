APP_NAME="حوات إحصاء"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://ihsa.test
APP_TIMEZONE=Asia/Riyadh
APP_LOCALE=ar
APP_FALLBACK_LOCALE=ar
APP_FAKER_LOCALE=ar_SA

APP_MAINTENANCE_DRIVER=file
BCRYPT_ROUNDS=12

# لوحة الوزارة تُقدَّم من مضيف APP_URL. اضبط APP_DOMAIN فقط إن اختلف عن ذلك.
# APP_DOMAIN=hawat.sa

# بوابة المعلومات (مركز إدارة النظام) على مضيف مستقل — مثل info.hawat.sa.
# اتركه فارغًا لتقديمها تحت البادئة /info على نفس مضيف اللوحة.
INFO_PORTAL_DOMAIN=info.ihsa.test

# كلمة مرور حسابات البذر لبوابة المعلومات (UserSeeder). بيانات تهيئة لا إنتاج:
# غيّرها على الخادم قبل التسليم. الافتراضية hawat@2026 حين تُترك فارغة.
# SEED_PASSWORD=

LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=ihsa
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
# حين تعمل البوابة على مضيف مستقل اجعل هذه القيمة النطاق الأب المشترك
# (مثل ".hawat.sa") حتى تُقرأ الجلسة على المضيفين معًا.
SESSION_DOMAIN=.ihsa.test

FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
CACHE_STORE=file

MAIL_MAILER=log
MAIL_FROM_ADDRESS="noreply@ihsa.local"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"
